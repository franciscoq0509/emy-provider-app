export default (allCustomers, { text }) => {
    // console.log(allCustomers);
    // if(allCustomers.length !== 0){
    //     const keys = Object.keys(allCustomers);
    //     const filtered =  keys.filter(( k ) => {
    //         const textMatch = allCustomers[k].first_name === null || allCustomers[k].first_name.toLowerCase().includes(text.toLowerCase()) || 
    //         allCustomers[k].last_name === null || allCustomers[k].last_name.toLowerCase().includes(text.toLowerCase());
    //         return textMatch;
    //     });

    //} else 
    return allCustomers;
};